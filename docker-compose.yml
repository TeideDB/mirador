services:
  mirador:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - mirador-data:/data
    environment:
      - MIRADOR_DATA_DIR=/data
    restart: unless-stopped

volumes:
  mirador-data:
